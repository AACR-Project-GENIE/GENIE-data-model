% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/derive_drug_end_or_lastadm_int.R
\name{derive_drug_end_or_lastadm_int}
\alias{derive_drug_end_or_lastadm_int}
\title{Combine drug end date and last-administered date into one DOB-indexed interval}
\usage{
derive_drug_end_or_lastadm_int(drugs_enddt_int, drugs_lastdt_int, drugs_dc_ynu)
}
\arguments{
\item{drugs_enddt_int}{Numeric vector: days from DOB to drug end date
for a single drug slot (e.g. \code{drugs_enddt_int_1}).}

\item{drugs_lastdt_int}{Numeric vector: days from DOB to drug last
administered date for the same drug slot (e.g. \code{drugs_lastdt_int_1}).}

\item{drugs_dc_ynu}{Character vector of discontinuation status, already
mapped to labels by \code{\link[=map_drugs_dc_ynu]{map_drugs_dc_ynu()}}: one of \code{"Yes"}, \code{"No"},
\code{"Unknown, no documentation found"}.}
}
\value{
A numeric vector of the combined DOB-indexed end-or-last-admin
day, the same length as the inputs.
}
\description{
For a single drug slot, returns the DOB-indexed day on which the drug
either ended (if the drug was discontinued, \code{drugs_dc_ynu == "Yes"}) or
was last administered (if \code{drugs_dc_ynu} is \code{"No"} or
\code{"Unknown, no documentation found"}). Parallels the logic of
\code{dx_drug_end_or_lastadm_int} in the legacy derivation, but stays on the
DOB-indexed scale (as the \verb{drugs_*} columns do) rather than subtracting
the diagnosis date.
}
\details{
Intended to be applied one drug slot at a time (e.g. drug 1, drug 2, ...)
by the caller, to produce \verb{drugs_drug_end_or_lastadm_int_<n>} columns.
}
\examples{
derive_drug_end_or_lastadm_int(
  drugs_enddt_int = c(100, NA, 200),
  drugs_lastdt_int = c(NA, 150, NA),
  drugs_dc_ynu = c("Yes", "No", "Unknown, no documentation found")
)
}
