% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/derive_regimen_drugs.R
\name{derive_regimen_drugs}
\alias{derive_regimen_drugs}
\title{Derive regimen_drugs (alphabetized drug-name list per regimen)}
\usage{
derive_regimen_drugs(dat)
}
\arguments{
\item{dat}{A regimen-level data frame containing the wide \verb{drugs_drug_<n>}
drug-name columns. Typically the output of earlier \code{build_dv_tab_reg()} steps,
after \code{map_drug_name()} has replaced drug codes with names.}
}
\value{
\code{dat} with a \code{regimen_drugs} character column added.
}
\description{
For each regimen row, collapses the individual \verb{drugs_drug_<n>} drug-name
columns into a single comma-separated \code{regimen_drugs} string listing the
drugs in alphabetical order. Parallels the legacy derivation in
\code{create_derived_vars.Rmd}: take the short name (everything before the first
\code{(}), sort, and concatenate with \code{", "}. "Other" free-text drug columns
(\verb{drugs_drug_oth*}) and mask columns are not included.
}
\examples{
# derive_regimen_drugs(reg)
}
