% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/derive_stage_dx_nsclc_2.R
\name{derive_stage_dx_nsclc_2}
\alias{derive_stage_dx_nsclc_2}
\title{Derive stage_dx and stage_dx_iv for NSCLC using group stage fallback}
\usage{
derive_stage_dx_nsclc_2(dat)
}
\arguments{
\item{dat}{A data frame of NSCLC cancer diagnoses that already has
\code{stage_dx} and \code{stage_dx_iv} columns (from \code{\link[=derive_stage_dx]{derive_stage_dx()}}).
Must also contain: \code{best_ajcc_stage_cd}, \code{ca_stage_iv}, \code{ca_stage},
\code{ca_clin_group_stage}, \code{ca_path_group_stage}, and
\code{ca_tx_pre_path_stage}.}
}
\value{
\code{dat} with \code{stage_dx} and \code{stage_dx_iv} updated for rows where
the group stage fallback produced a result.
}
\description{
For NSCLC patients whose \code{stage_dx} is still \code{NA} after the universal
AJCC-based derivation in \code{\link[=derive_stage_dx]{derive_stage_dx()}}, attempts to fill in stage
using clinical and pathologic group staging. Rows that already have a
\code{stage_dx} value are left unchanged.
}
\details{
Unlike \code{\link[=derive_stage_dx_nsclc]{derive_stage_dx_nsclc()}}, this version uses \code{ca_clin_group_stage}
and \code{ca_path_group_stage} directly rather than reconstructing from
individual T/N components.

The priority between clinical and pathologic depends on
\code{ca_tx_pre_path_stage}: if \code{"Yes"} (treatment preceded pathologic
staging), clinical is preferred with pathologic as fallback; if
\code{"No"}, pathologic is preferred with clinical as fallback.
}
\examples{
# derive_stage_dx_nsclc_2(ca_ind_nsclc)
}
