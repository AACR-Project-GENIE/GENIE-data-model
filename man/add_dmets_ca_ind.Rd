% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_dmets_ca_ind.R
\name{add_dmets_ca_ind}
\alias{add_dmets_ca_ind}
\title{Derive all distant metastasis variables and attach to index cancer}
\usage{
add_dmets_ca_ind(tables, cohort_ca_types, only_full_dmet_scans = FALSE)
}
\arguments{
\item{tables}{A named list of derived data frames, expected to contain
\code{ca_ind}, \code{img}, and \code{path}.}

\item{cohort_ca_types}{Character vector of \code{path_ca_type} values that
correspond to the cohort's cancer type (e.g.
\code{c("Non Small Cell Lung Cancer", "Lung Cancer, NOS")} for NSCLC).}

\item{only_full_dmet_scans}{If \code{TRUE}, only include imaging scans where
every site is classified as Distant (via \code{\link[=derive_scan_dmets_long_2]{derive_scan_dmets_long_2()}}).
If \code{FALSE} (default), include all scans with any Distant site (via
\code{\link[=derive_scan_dmets_long]{derive_scan_dmets_long()}}).}
}
\value{
The \code{tables} list with \code{ca_ind} updated to include \verb{dmets_*},
\verb{dx_to_dmets_*_days}, \code{dmets_stage_i_iii}, and \code{dx_to_dmets_days}.
}
\description{
Orchestrates the full distant mets pipeline: imaging scans (stream A),
pathology reports (stream B), at-diagnosis mets (stream C), then
combines across streams and adds overall Stage I-III flags. The result
is joined onto \code{tables$ca_ind}.
}
\examples{
# tables <- add_dmets_ca_ind(tables, c("Non Small Cell Lung Cancer", "Lung Cancer, NOS"))
}
