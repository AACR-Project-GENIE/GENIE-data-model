% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/derive_path_dmets_long.R
\name{derive_path_dmets_long}
\alias{derive_path_dmets_long}
\title{Identify distant metastases from pathology reports}
\usage{
derive_path_dmets_long(
  path,
  ca_ind,
  cohort_ca_types,
  include_indeterminate = FALSE
)
}
\arguments{
\item{path}{Pathology data with mapped columns (after \code{\link[=build_dv_tab_path]{build_dv_tab_path()}}
has been applied). Must contain \verb{path_ca*}, \verb{path_ca_type*}, and
\verb{path_site*} columns.}

\item{ca_ind}{Index cancer subset of the cancer diagnosis form. Must contain
\code{record_id}, \code{ca_seq}, \code{ca_cadx_int}, and \code{stage_dx}.}

\item{cohort_ca_types}{Character vector of \code{path_ca_type} values that
correspond to the cohort's cancer type (e.g.
\code{c("Non Small Cell Lung Cancer", "Lung Cancer, NOS")} for NSCLC).}

\item{include_indeterminate}{If \code{TRUE}, include sites with \code{NA}
classification (Indeterminate) alongside Distant. Default \code{FALSE}.}
}
\value{
A long data frame with potentially multiple rows per
patient-cancer-site group, including columns \code{mets_site_group},
\code{classification}, and \code{path_proc_int}.
}
\description{
Pivots \verb{path_ca_type*}, \verb{path_ca*}, and \verb{path_site*} columns long by
specimen number, filters to specimens with invasive cancer matching the
cohort, classifies each site using \code{\link[=load_nsclc_met_site_groups]{load_nsclc_met_site_groups()}}, and
keeps only distant metastases. Left-joined back to the index cancer so
every patient gets at least one row. A patient may have multiple rows per
\code{mets_site_group} if distant mets at the same site were observed on
different path reports — downstream consumers should reduce to the earliest
\code{path_proc_int} per site group as needed.
}
\examples{
# derive_path_dmets_long(path, ca_ind, c("Non Small Cell Lung Cancer", "Lung Cancer, NOS"))
}
