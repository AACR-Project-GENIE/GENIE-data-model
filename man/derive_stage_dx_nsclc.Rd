% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/derive_stage_dx_nsclc.R
\name{derive_stage_dx_nsclc}
\alias{derive_stage_dx_nsclc}
\title{Derive stage_dx and stage_dx_iv for NSCLC using TNM fallback}
\usage{
derive_stage_dx_nsclc(dat)
}
\arguments{
\item{dat}{A data frame of NSCLC cancer diagnoses that already has
\code{stage_dx} and \code{stage_dx_iv} columns (from \code{\link[=derive_stage_dx]{derive_stage_dx()}}).
Must also contain: \code{best_ajcc_stage_cd}, \code{ca_stage_iv}, \code{ca_stage},
\code{ca_clin_t_stage}, \code{ca_clin_t1_det}, \code{ca_clin_t2_det},
\code{ca_clin_n_stage}, \code{ca_path_group_stage}, \code{ca_path_t_stage},
\code{ca_path_t1_det}, \code{ca_path_t2_det}, \code{ca_path_n_stage}, and
\code{ca_tx_pre_path_stage}.}
}
\value{
\code{dat} with \code{stage_dx} and \code{stage_dx_iv} updated for rows where
the TNM fallback produced a result.
}
\description{
For NSCLC patients whose \code{stage_dx} is still \code{NA} after the universal
AJCC-based derivation in \code{\link[=derive_stage_dx]{derive_stage_dx()}}, attempts to fill in stage
using clinical and pathologic TNM group staging (7th edition AJCC rules
for lung cancer). Rows that already have a \code{stage_dx} value are left
unchanged.
}
\details{
The priority between clinical and pathologic TNM depends on
\code{ca_tx_pre_path_stage}: if \code{"Yes"} (treatment preceded pathologic
staging), clinical TNM is preferred with pathologic as fallback; if
\code{"No"}, pathologic is preferred with clinical as fallback.
}
\examples{
# derive_stage_dx_nsclc(ca_ind_nsclc)
}
