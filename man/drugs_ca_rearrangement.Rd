% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drugs_ca_rearrangement.R
\name{drugs_ca_rearrangement}
\alias{drugs_ca_rearrangement}
\title{Rearrange drugs-by-cancer indicator columns into long form}
\usage{
drugs_ca_rearrangement(dat, prefix = "^drugs_ca")
}
\arguments{
\item{dat}{A regimen-level data frame containing the wide \verb{drugs_ca*}
indicator columns and a \code{redcap_repeat_instance} column.}

\item{prefix}{A regex used to identify the \verb{drugs_ca*} indicator columns to
pivot. Defaults to \code{"^drugs_ca"}.}
}
\value{
\code{dat} pivoted to long form with one row per regimen-cancer
association, a new integer \code{redcap_ca_seq} column placed after
\code{redcap_repeat_instance}, and the original wide indicator columns removed.
}
\description{
The raw regimen form has one row per regimen with a set of wide \verb{drugs_ca*}
indicator columns, one per cancer diagnosis, marking which cancer(s) a
regimen was directed at. This helper pivots those indicators into long form
so that each row represents one (regimen, cancer) association, keyed by a new
\code{redcap_ca_seq} column parsed from the original column names. Rows where the
indicator is not \code{1} (i.e. regimens not directed at that cancer) are dropped.
}
\examples{
# drugs_ca_rearrangement(reg)
}
